name: Talk Weather

on:
  push:
    branches: main
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  ask-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Get the weather
        run: |
          wttr=https://wttr.in/Paris
          echo $wttr